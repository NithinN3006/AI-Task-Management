# Copy this file to application.yml or set these as environment variables
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

jwt:
  secret: your_secure_jwt_secret_replace_in_production
  expiration: 3600000 # 1 hour

gemini:
  api:
    key: your_gemini_api_key_here
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent
